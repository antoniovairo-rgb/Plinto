# La chiave di firma non entra mai in un repository pubblico.
# Perderla e' una scocciatura; pubblicarla e' un incidente.
*.keystore
*.jks
keystore.properties

# Prodotti della compilazione e stato locale: si rigenerano.
.gradle/
build/
app/build/
local.properties
*.aab
*.apk
